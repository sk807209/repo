FROM node:10

WORKDIR /usr/src/app
COPY package.json ./


RUN npm install

COPY . .


EXPOSE 8081
CMD ["node", "server.js"]


Install docker on your machine
enable Docker CLI for the non-root user account that will be used to run vs code

To install the extensions ,open extensions view search for docker and instal it.
Now you can see all your running containers installed in vs code. 
